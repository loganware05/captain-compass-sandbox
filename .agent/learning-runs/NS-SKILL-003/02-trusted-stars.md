# 02 — Confirm trusted Star provenance

Source: sandbox `tests/fixtures/ti/github-stars-recorded` (fixtures mode).

Selected candidates:
- `example-org/python-settings-secrets-hygiene` (primary) — pydantic settings, env-only secrets, key-path guards, fail-closed auth
- `example-org/httpx-resilient-clients` (secondary) — retries/timeouts/explicit failure modes

No live clone or execution of external repositories.
